<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Steuerelemente"
             x:Class="Steuerelemente.MainPage">

    <StackLayout>
        <Label Text="Mein erstes Label"
               TextColor="Blue"
               FontSize="30"/>
        <Button Text="Klick mich!" BorderRadius="20" BackgroundColor="Green" Clicked="Button_Clicked"/>
        <!-- TextBox -->
        <Entry x:Name="entryUsername" Placeholder="Username" Keyboard="Numeric" />
        <Entry x:Name="entryPasswort" Placeholder="Passwort" IsPassword="True" />
        <!-- Multiline-TextBox -->
        <Editor HeightRequest="150"/>
        <!-- CheckBox -->
        <Switch IsToggled="True"/>
        <Slider Minimum="0" Maximum="100" Value="30"/>
        <!-- Minimum = 0, Maximum = 1 -->
        <ProgressBar Progress="0.333"/>
        <Stepper Minimum="0" Maximum="100" Increment="10" />
        
        <!-- ComboBox -->
        <Picker>
            <Picker.Items>
                <x:String>Apfel</x:String>
                <x:String>Birne</x:String>
                <x:String>Banane</x:String>
            </Picker.Items>
        </Picker>
        <BoxView HeightRequest="10" BackgroundColor="White"/>
        <DatePicker />
        <TimePicker />

    </StackLayout>

</ContentPage>
